# Sistema operacional
.DS_Store
Thumbs.db
desktop.ini

# Dependências / build (não usados, por precaução)
node_modules/

# Artefatos de empacotamento
*.zip

# Arquivos de runtime do LevelDB (recriados pelo Foundry)
packs/*/LOCK
packs/*/LOG
packs/*/LOG.old
